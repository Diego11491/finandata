[project]
name = "finandata-trustgate"
version = "1.1.2"
description = "Pipeline ETL financiero con Prefect, calidad, auditoría y compuerta regulatoria"
requires-python = ">=3.10,<3.14"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
